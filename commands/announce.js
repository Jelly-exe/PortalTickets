const Discord = require("discord.js");
const fs = require("fs");
const config = require("../config-files/main.json");

module.exports.run = async (client, message, args) => {

    // Steal announcement command from frozenwaters

    // Command to be transfered to Portal | General/Moderation

    // Make it so manager and above can use the command

    // Remember to change the bottom message part to be Portal Services Management instead of frozenwaters

    // Some roles will need changing, announcement channel Id.
}
module.exports.config = {
  name: "announce",
  aliases: ["announcement"]
}